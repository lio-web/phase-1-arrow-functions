const divide=(a,b)=>a/b;
  divide(2000,100);
const square = x=>(x**2);
 square(2)
const add = (a,b) =>a+b;
 add(3,4);
 const sum =(a, b) => {
  console.log(`Adding ${a}`);
  console.log(`Adding ${b}`);
  return a + b;
}
sum(3,4); //=> 7